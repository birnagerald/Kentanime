{% extends 'baseAdmin.html.twig' %}

{% block title %}Éditer {{ category.title }}{% endblock %}
{% block body_id 'blog_index' %}

{% block main %}  
<div class="container mt-4">
    <h1>Éditer la catégorie : {{ category.title }}</h1>

    {{ include('admin/category/_form.html.twig', {button: 'Éditer'}) }}

    <a href="{{ path('admin_category_index') }} " class="btn btn-secondary">Retour à la liste</a>

    {# {{ include('admin/category/_delete_form.html.twig') }} #}
    </div>
{% endblock %}
