{% extends 'baseAdmin.html.twig' %}

{% block title %}Éditer un commentaire{% endblock %}
{% block body_id 'blog_index' %}

{% block main %}
<div class="container mt-4">
    <h1>Éditer le commentaire : {{ comment.id }}</h1>

    {{ include('admin/comment/_form.html.twig', {button: 'Éditer'}) }}

    <a href="{{ path('admin_comment_index') }} " class="btn btn-secondary">Retour à la liste</a>

</div>
{% endblock %}